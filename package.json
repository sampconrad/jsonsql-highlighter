{
  "name": "jsonsqlite-highlighter",
  "displayName": "SQLite Highlighter for JSON",
  "description": "Enhanced syntax highlighting for SQLite queries embedded in JSON files with distinct visual elements for strings, variables, and SQL keywords",
  "version": "1.0.5",
  "author": "sampconrad <https://github.com/sampconrad>",
  "homepage": "https://github.com/sampconrad",
  "publisher": "sampconrad",
  "repository": {
    "type": "git",
    "url": "https://github.com/sampconrad/jsonsqlite-highlighter.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Programming Languages",
    "Other"
  ],
  "activationEvents": [
    "onLanguage:json"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "sqlite-in-json",
        "aliases": ["SQLite in JSON", "sqlite-json"],
        "extensions": [".json"],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "sqlite-in-json",
        "scopeName": "source.sqlite-in-json",
        "path": "./syntaxes/sqlite-in-json.tmLanguage.json"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "16.x",
    "@types/vscode": "^1.74.0",
    "typescript": "^4.9.4",
    "@vscode/vsce": "^2.19.0"
  }
}
